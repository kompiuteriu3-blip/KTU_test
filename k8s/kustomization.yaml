namespace: webapp

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - httproute.yaml

generatorOptions:
  disableNameSuffixHash: false

configMapGenerator:
  - name: webapp-config
    literals:
      - MESSAGE=Testuojam ArgoCD dabar
      - PORT=8080

images:
  - name: harbor.apps.kubernetes-okd.digidefence.ktu.edu/library/webapp
    newName: harbor.apps.kubernetes-okd.digidefence.ktu.edu/library/webapp
    newTag: 0.1.3
